#include <bits/stdc++.h> // Includes all c++ libraries
// #include <iostream>
// #include <string>

using namespace std; // other wise you need to use std:: all over the program..

int main() {
    string name;
    int age;

    // reading from input.txt
    cin >> name >> age; //we can enter input in one after other lines also, not mandatory to after space

    // writing to output.txt
    cout << "Hello " << "\n" << name << "! You are " << age << " years old." << endl;  
    /*for new line in output (1)using  : "\n" 
                             (2) using : endl */

    cout << "how are you??";
    return 0;
}
                    